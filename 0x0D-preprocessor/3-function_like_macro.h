#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * Task: Write a function-like macro ABS(x) that...
 * ...computes the absolute value of a number x.
 * Filename: 3-function_like_macro.h
 * File Descriptuon: a header file tgat defines the function-like macro.
 */

#define ABS(x) (((x) >= (0)) ? (x) : -(x))

#endif
